<!DOCTYPE html>
<% if DEBUG %>
<html lang="en">
<% else %>
<html lang="en" manifest="cache.manifest">
<% end %>
	<head>
		<meta http-equiv="Content-type" content="text/html; charset=utf-8" />
		<title>Doneski</title>
		<meta name="keywords" content="todo list, productivity, doneski"/>
		<meta name="description" content="Doneski is an app for to-do lists, simple as that. It's dirt simple, easy to use, and works seriously everywhere." />
		<meta name="author" content="Seabright Studios Ltd."/>
		
		<meta name="robots" content="noindex">
		
		<meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="black" />
		<link rel="apple-touch-icon" href="/images/icon-dark.png" />
		<link rel="apple-touch-startup-image" href="/images/splash.png" />
		
		<link rel="icon" href="data:image/png;base64,<%= $doneskifavicon ||= Seabright::Stylesheet.b64("/images/doneski.png")%>" type="image/png" />
		<link rel="shortcut icon" href="data:image/png;base64,<%= $doneskifavicon ||= Seabright::Stylesheet.b64("/images/doneski.png")%>" type="image/png" />
		<% if COMPRESS_ASSETS %>
		<style>
		<%= $doneskicss ||= Seabright::Stylesheet.new(IO.read('static/stylesheets/doneski.css')).minified%>
		<%= $papercss ||= Seabright::Stylesheet.new(IO.read('static/stylesheets/paper.css')).minified%>
		</style>
		<% else %>
		<link href="/stylesheets/doneski.css" media="screen" rel="stylesheet" type="text/css" />
		<link href="/stylesheets/paper.css" media="screen" rel="stylesheet" type="text/css" />
		<% end %>
	</head>
	<body class="init">
		<content>
			<header class="initial">
				<logo>Doneski</logo>
				<tagline>The dirt simplest to-do app that works everywhere.</tagline>
			</header>
			<section>
				<lists></lists>
			</section>
			<toolbar class="delayed">
				<listnav class="delayed tight0"></listnav>
				<buttons>
					<button class="config"></button>
				</buttons>
			</toolbar>
		</content>
	</body>
	<% if COMPRESS_ASSETS %>
	<script type="text/javascript">
	<%= $doneskijs ||= Closure::Compiler.new.compile(File.open('static/javascripts/doneski.js', 'r'))%>
	<%= $touchyjs ||= Closure::Compiler.new.compile(File.open('static/javascripts/touchyfeely.js', 'r'))%>
	</script>
	<% elsif USE_MINIFIED %>
	<script type="text/javascript" src="/javascripts/doneski-min.js"></script>
	<script type="text/javascript" src="/javascripts/touchyfeely-min.js"></script>
	<% else %>
	<script type="text/javascript" src="/javascripts/doneski.js"></script>
	<script type="text/javascript" src="/javascripts/touchyfeely.js"></script>
	<% end %>
	<script language="javascript">
	if(!('localStorage' in window && window['localStorage'] !== null && localStorage["doneski.lists"])) document.body.className = document.body.className.replace("init","");
	</script>
</html>